<?php
    $server1='localhost';
    $username1='groupa'; //address this
    $password1='password'; //address this, this should be randomly generated and hidden from the user
    $database1='groupa'; //address this
    try{
        $db1 = new PDO('mysql:host='.$server1.';dbname='.$database1.';charset=utf8',$username1,$password1);
    } catch(PDOException $ex) {
        echo "Error connecting to the database."; //if not connected
    }
?>